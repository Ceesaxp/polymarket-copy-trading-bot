# ============================================================================
# Polymarket Copy Trading Bot - Configuration File
# ============================================================================
# Copy this file to .env and fill in your values
# NEVER commit your .env file to git (it contains sensitive information)

# ============================================================================
# REQUIRED SETTINGS (Must fill these in)
# ============================================================================

# Your wallet's private key (64-character hex string, no 0x prefix)
#  KEEP THIS SECRET! Never share or commit to git!
# Example: 0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
PRIVATE_KEY=your_private_key_here

# Your wallet address (40-character hex string, can include or exclude 0x prefix)
# This should match the wallet from your PRIVATE_KEY
# Example: 0x1234567890123456789012345678901234567890
# Or:     1234567890123456789012345678901234567890
FUNDER_ADDRESS=your_wallet_address_here

# ============================================================================
# TRADER MONITORING (Choose ONE method)
# ============================================================================

# METHOD 1: Multiple traders (NEW - recommended for flexibility)
# Comma-separated list of trader addresses to copy
# Example: TRADER_ADDRESSES=204f72f35326db932158cba6adff0b9a1da95e14,def456abc123789012345678901234567890abcd
# TRADER_ADDRESSES=

# METHOD 2: Single trader (LEGACY - still supported for backward compatibility)
# The whale address you want to copy trades from (40-character hex, no 0x prefix)
# Find successful traders on Polymarket and use their wallet address
# Example: 204f72f35326db932158cba6adff0b9a1da95e14
TARGET_WHALE_ADDRESS=target_whale_address_here

# METHOD 3: JSON configuration file (ADVANCED)
# For per-trader settings (scaling_ratio, min_shares, enabled), create traders.json
# See traders.json.example for format
# The bot will check for traders.json automatically if it exists

# WebSocket RPC Provider (choose ONE - Alchemy is recommended for beginners)
# Get free API key from: https://www.alchemy.com/
ALCHEMY_API_KEY=your_alchemy_api_key_here

# Alternative: Use Chainstack instead of Alchemy (leave ALCHEMY_API_KEY empty)
# Get free API key from: https://chainstack.com/
# CHAINSTACK_API_KEY=your_chainstack_api_key_here

# ============================================================================
# TRADING SETTINGS (Optional - defaults shown)
# ============================================================================

# Enable actual trading (true) or monitoring only (false)
# Set to false when testing to prevent accidental trades
ENABLE_TRADING=true

# Mock trading mode - simulates trades without actually executing them
# Good for testing: Set to true to see what the bot would do
MOCK_TRADING=false

# ============================================================================
# CIRCUIT BREAKER SETTINGS (Advanced - Optional)
# ============================================================================
# These protect you from copying trades in dangerous market conditions

# Minimum trade size (shares) to trigger circuit breaker analysis
# Default: 1500.0 (trades below this skip circuit breaker)
CB_LARGE_TRADE_SHARES=1500.0

# Number of consecutive large trades that trigger circuit breaker
# Default: 2 (if 2+ large trades in window, check order book depth)
CB_CONSECUTIVE_TRIGGER=2

# Time window (seconds) to check for consecutive large trades
# Default: 30 (checks last 30 seconds)
CB_SEQUENCE_WINDOW_SECS=30

# Minimum order book depth (USD) required beyond whale's price
# If depth is lower, circuit breaker blocks the trade
# Default: 200.0
CB_MIN_DEPTH_USD=200.0

# How long (seconds) to block trades after circuit breaker trips
# Default: 120 (2 minutes)
CB_TRIP_DURATION_SECS=120

# ============================================================================
# NOTES
# ============================================================================
#
# 1. All values with "here" need to be replaced with your actual values
# 2. Remove the "#" at the start of a line to enable that setting
# 3. Boolean values: true/false (case-insensitive) or 1/0
# 4. Leave optional settings as-is if you're not sure what they do
# 5. See docs/CONFIGURATION.md for detailed explanations

# Persistence
DB_ENABLED=true              # Enable SQLite persistence
DB_PATH=trades.db            # Database file path

# Multi-Trader
TRADER_ADDRESSES=addr1,addr2 # Comma-separated addresses
# Or use traders.json file

# Aggregation
AGG_ENABLED=true             # Enable trade aggregation
AGG_WINDOW_MS=800            # Aggregation window
AGG_BYPASS_SHARES=4000       # Bypass threshold

# API
API_ENABLED=false            # Enable HTTP API
API_PORT=8080                # API port

# ============================================================================
# PORTFOLIO-BASED BET SIZING (Optional - Risk Management)
# ============================================================================
# Limit maximum bet size as a percentage of total portfolio value
# Portfolio value = USDC balance + positions value
# This helps prevent oversized bets as your capital grows

# Maximum bet as percentage of portfolio (e.g., 0.02 = 2%)
# Leave empty or remove to disable portfolio-based limiting
# Recommended range: 0.01 (1%) to 0.05 (5%)
# Example: With $5000 portfolio and 0.02 setting, max bet = $100
# MAX_BET_PORTFOLIO_PERCENT=0.02

# How long to cache portfolio value (seconds)
# Lower = more accurate but more API calls
# Higher = less API calls but potentially stale data
# Default: 300 (5 minutes)
# PORTFOLIO_CACHE_SECS=300

# ============================================================================
# BUILDER CREDENTIALS (Required for auto_claim / redemption)
# ============================================================================
# Apply at: https://docs.polymarket.com/developers/builders
# These enable gasless redemption of winning positions

# POLY_BUILDER_API_KEY=your_builder_api_key
# POLY_BUILDER_SECRET=your_builder_secret
# POLY_BUILDER_PASSPHRASE=your_builder_passphrase

#
